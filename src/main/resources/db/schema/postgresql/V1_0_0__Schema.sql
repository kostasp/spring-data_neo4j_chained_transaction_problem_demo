

create table box(
id BIGINT,
name text,
description text
);

CREATE SEQUENCE box_seq START with 1 NO  CYCLE;

create table cargo(
id BIGINT,
name text,
description text
);

CREATE SEQUENCE cargo_seq START with 1 NO  CYCLE;

